<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Live currency rates fetch -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Floating calculator overlay window -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Needed so the overlay Service can start from background on Android 14+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".SHVApplication"
                android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_uc"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeEmptyActivity">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:resizeableActivity="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboardHidden|navigation|uiMode|colorMode|density"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.ComposeEmptyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Floating calculator overlay service -->
        <service
            android:name=".overlay.FloatingCalcService"
            android:exported="false" />

    </application>

</manifest>
