package com.example.omnicortex.ui.theme

import androidx.compose.ui.graphics.Color

// ── AMOLED base ───────────────────────────────────────────────────────────────
val BgAmoled     = Color(0xFF000000)
val BgCard       = Color(0xFF0D1117)
val BgCardBorder = Color(0xFF1E2730)
val BgElevated   = Color(0xFF161B22)
val NavBg        = Color(0xFF090E14)

// ── Aegis accent palette ──────────────────────────────────────────────────────
val AegisGreen   = Color(0xFF00E5A0)   // primary — safe / good
val AegisCyan    = Color(0xFF00C8E0)   // network module
val AegisAmber   = Color(0xFFFFB300)   // warnings
val AegisRed     = Color(0xFFFF3D5A)   // critical / danger
val AegisPurple  = Color(0xFFBB86FC)   // breach module
val AegisBlue    = Color(0xFF4FC3F7)   // comms module
val AegisOrange  = Color(0xFFFF7043)   // high severity

// ── Text ──────────────────────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFF0F4F8)
val TextSecondary = Color(0xFF8B96A5)
val TextMuted     = Color(0xFF4A5568)

// ── Score arc colours (gradient stops) ───────────────────────────────────────
val ScoreRed    = Color(0xFFFF3D5A)
val ScoreAmber  = Color(0xFFFFB300)
val ScoreGreen  = Color(0xFF00E5A0)
